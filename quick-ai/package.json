{
  "name": "quick-ai",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "QUICK//AI — task-to-AI decision engine. Right AI. Right task.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "playwright-core": "^1.62.1",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
